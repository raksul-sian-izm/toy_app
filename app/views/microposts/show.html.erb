<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @micropost.content %>
</p>

<p>
  <strong>Desctiption:</strong>
  <% @micropost.desc_images.each do |desc_img| %>
    <%= image_tag desc_img.variant(resize_to_limit: [100, 100]) %>
  <% end %>
</p>

<p>
  <strong>User:</strong>
  <%= @micropost.user_id %>
</p>

<%= link_to 'Edit', edit_micropost_path(@micropost) %> |
<%= link_to 'Back', microposts_path %>
